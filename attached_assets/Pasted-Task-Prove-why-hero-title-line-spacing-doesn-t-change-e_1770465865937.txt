Task: Prove why hero title line spacing doesn’t change even with extreme leading values
Mode: DIAGNOSE WITH EVIDENCE — minimal temporary instrumentation allowed
Do not redesign. Do not propose final fix yet.
Step 1 — Confirm the rendered element is the one we edit
Search in repo for: data-testid="hero-title"
Output: all matches with file+line.
Search for the Arabic string: مجتمع طلابي يناقش
Output: all matches with file+line.
Confirm that the visible headline in Landing comes from the same <h1> we edit in client/src/pages/landing.tsx.
Step 2 — Force an unmistakable visual change (to verify compile/apply)
In landing.tsx on the Arabic <h1> path ONLY, temporarily add these classes:
outline outline-4 outline-red-500
bg-yellow-200/30
text-[72px]
Expected: outline + background + huge font MUST appear immediately.
If not: we are not editing the active component/origin.
Report: whether these visual markers appear in:
Replit Preview
External browser (published/dev URL)
Step 3 — Runtime computed style dump (must be real values)
Add TEMP code right after render (inside Landing component, useEffect) to log computed styles:
Select: document.querySelector('[data-testid="hero-title"]')
Log computed:
fontSize
lineHeight
fontFamily
display
paddingTop/paddingBottom
transform (if any)
Also log: element.className
Return the console output exactly (copy/paste).
Step 4 — Check Tailwind JIT actually generates the class
Confirm Tailwind content paths include:
client/src/**/*.{ts,tsx}
Run/search build output (or check DevTools Styles panel):
Is there a rule for .leading-\[2\.0\] ?
If absent, Tailwind didn’t generate it.
Return evidence:
Either DevTools screenshot of Styles showing leading-[2.0] rule
Or proof it does not exist
Step 5 — Check if another rule overrides line-height
In DevTools for the <h1>:
In Computed → line-height
Show the winning CSS rule source (which selector/file)
List any competing rules affecting line-height (even inherited)
Deliverable
Provide a short report:
Is the edited <h1> the one rendered? (YES/NO + evidence)
Did the forced outline/background appear? (YES/NO)
Actual computed line-height in px (RTL + LTR)
Is leading-[2.0] CSS rule generated by Tailwind? (YES/NO)
If overridden: identify the overriding selector + source